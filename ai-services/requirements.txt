"""
COGNARC — ai-services Phase-3 MVP Dependencies
ai-services/requirements.txt

T1.2: groq, pydantic>=2.0, tenacity, httpx — raw SDK only, no frameworks.
§07: NO LangChain, NO LangGraph, NO LlamaIndex. Direct HTTP/Groq SDK only.
"""
# ── Core AI Client ────────────────────────────────────────────
groq>=0.30.0,<1.0.0           # Groq Python SDK — direct chat.completions API

# ── Data Validation ───────────────────────────────────────────
pydantic>=2.0,<3.0             # Pydantic v2 for output parsing + validation

# ── Retry Logic ───────────────────────────────────────────────
tenacity>=8.2.0,<9.0.0         # Retry with exponential backoff (max 2 attempts)

# ── HTTP Client / Timeout Wrapper ─────────────────────────────
httpx>=0.27.0,<1.0.0           # httpx.Timeout for 15s read timeout wrapper

# ── Observability ─────────────────────────────────────────────
langfuse>=2.0.0,<3.0.0         # Langfuse tracing for all Groq calls (tokens, latency)
